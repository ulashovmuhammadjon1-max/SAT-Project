# Test 20 — build record

Test 20 is **PUBLISHED** in production, 147 questions.

Tests 19, 20 and 21 were built together in one pass. The shared record — pipeline, dedupe
corpora, verifier coverage, per-test results and the questions rewritten rather than accepted —
is in [`../test-19-build/MANIFEST.md`](../test-19-build/MANIFEST.md). The equivalent record for
the previous group is in [`../test-16-build/MANIFEST.md`](../test-16-build/MANIFEST.md).

This directory holds Test 20's own sources: `math_test20.py`, `verify_math_test20.py`,
`rw_test20.py`, `balance_rw.py`, `assemble_test20.py`, `insert_test.mjs` and the assembled
`test20.json`.
