# Test 18 — build record

Test 18 is **PUBLISHED** in production, 147 questions.

Tests 16, 17 and 18 were built together in one pass, and the full record — pipeline, dedupe
corpora, verifier coverage, per-test similarity results, the questions rewritten rather than
accepted, and the post-publish checks read back from production — is in
[`../test-16-build/MANIFEST.md`](../test-16-build/MANIFEST.md).

This directory holds Test 18's own sources: `math_test18.py`, `verify_math_test18.py`,
`rw_test18.py`, `balance_rw.py`, `assemble_test18.py`, `insert_test.mjs` and the assembled
`test18.json`.
